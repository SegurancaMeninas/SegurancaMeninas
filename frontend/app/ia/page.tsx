import { GenericMetricDashboard } from '@/components/dashboard-client';

export default function Page() {
  return <GenericMetricDashboard title="Inteligência Artificial" description="Detecção automática, precisão e remoção proativa quando a plataforma divulgar esse detalhamento." endpoint="indicators" />;
}
